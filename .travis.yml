language: cpp
compiler:
  - gcc
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y apache2-prefork-dev
script: make clean all
deploy:
  provider: releases
  api_key: "d5ba3fc405e54709db494c67c912b2d0dd1331d2"
  file: "./src/.libs/mod_less.so"
  skip_cleanup: true
  on:
    tags: true
